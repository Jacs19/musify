export class Artist{
    //esto es lo mismo que realizar el paso de parametros con 'this'
    constructor(
        public name: string,
        public description: string,
        public image: string
    ){}
}